package com.course.repository;

import com.course.model.addUserCase;
import org.springframework.data.jpa.repository.JpaRepository;

public interface addUserCaseRepo extends JpaRepository<addUserCase,Integer> {
}
